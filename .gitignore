# System files
.DS_Store
Thumbs.db

# Environment variables
# IMPORTANT: Never commit your .env file if it contains sensitive secrets.
# For public variables managed by Next.js (prefixed with NEXT_PUBLIC_),
# they are intended to be bundled, but the .env file itself should be ignored.
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
logs

# Next.js
.next/
out/

# Supabase local development
supabase/.supabase/
supabase/functions/.env*
supabase/db/shadow/
supabase/volumes/

# IDEs and editors
.vscode/
!.vscode/extensions.json # Allow committing recommended extensions
!.vscode/settings.json   # Allow committing project-specific settings if desired, otherwise keep .vscode/ ignored
.idea/

# Build artifacts
dist/
build/
coverage/

# Other
*.pem
*.key
